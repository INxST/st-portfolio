---
import Layout from '@/layouts/Layout.astro';
import WorksSlide from '@/components/WorksSlide';
import works from '@/data/works';
import updatePath from '@/libs/updatePath';
const path = updatePath();
const title = '制作実績';
const titleEn = 'WORKS';
---

<Layout title={titleEn} maxW={true} isOverflowYHidden={true}>
  <WorksSlide
    title={title}
    titleEn={titleEn}
    items={works}
    path={path}
    client:only="react"
  />
</Layout>

<style lang="scss">
  .ts-text-link {
    span {
      justify-content: center;

      gap: 0.2rem;
      @media screen and (min-width: 768px) {
        gap: 0.3rem;
      }
    }
  }
</style>
