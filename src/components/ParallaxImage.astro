---
interface Props {
  src: string;
  alt: string;
}
const { src, alt } = Astro.props;
---

<picture
  class="ts-horizontal-scroll-item ts-parallax block md:w-screen md:h-screen"
>
  <img src={src} alt={alt} class="md:h-full md:max-w-[80%] object-cover" />
</picture>
